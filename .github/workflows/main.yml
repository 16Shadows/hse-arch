name: CI
run-name: Doing CI...
on:
  push:
    branches:
      - integration
jobs:
  Build-services:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo
        uses: actions/checkout@v4
      - name: CD into source folder
        run: cd "Lab Work №5/src/api"
      - name: Start docker containers
        run: docker compose up &
      - name: Run integration tests
        run: ./test.sh